{
  "name": "plaid-local",
  "version": "1.0.0",
  "description": "Local Plaid app for financial analysis",
  "scripts": {
    "start": "concurrently \"pnpm run start-backend\" \"pnpm run start-frontend\"",
    "start-backend": "cd backend && ..\\..\\.venv\\Scripts\\python -m uvicorn main:app --reload --port 8000",
    "start-frontend": "cd frontend && pnpm install && pnpm run dev",
    "install-all": "pnpm install && cd frontend && pnpm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}